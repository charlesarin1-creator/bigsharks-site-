console.log("BIG SHARKS — Private Family Banking System Active");

const treasuryBalance = 100000;

const vaults = {
  stability: treasuryBalance * 0.40,
  operations: treasuryBalance * 0.25,
  growth: treasuryBalance * 0.15,
  bigsharks: treasuryBalance * 0.10,
  legacy: treasuryBalance * 0.10
};

console.log(vaults);
